server:
  port: 8083
spring:
  application:
    name: server-service
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/dst_servers
    username: ${DB_USERNAME:root}
    password: ${MYSQL_ROOT_PASSWORD:A4s7kQ2xP9mF3vR8}
