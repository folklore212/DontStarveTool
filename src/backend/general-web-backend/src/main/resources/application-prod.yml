app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:}

spring:
  flyway:
    clean-disabled: true

management:
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    root: INFO
