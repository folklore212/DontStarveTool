server:
  port: 8082
spring:
  application:
    name: template-service
  datasource:
    url: jdbc:mysql://localhost:3306/dst_templates
    username: root
    password: ${MYSQL_ROOT_PASSWORD:A4s7kQ2xP9mF3vR8}
  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD:Z8bT3rL5wN1qJ6yD}
steam:
  api:
    key: ${STEAM_API_KEY:}
